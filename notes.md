Things done:

- Using spot mid price as reference for swap (uses pre compiles)

Things to do:

- Lend excess liquidity to lending markets
- API wallets for order book strategy

Vault notes:
When using a vault, we do not use pool reserve vars.
